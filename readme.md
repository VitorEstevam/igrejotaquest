# igrejota quest crud

## como rodar:

* com python na máquina executar os comandos no console:
```
    pip install flask
    pip install psycopg2 
```

* atualizar as credenciais do banco em connections/connection.py

* no diretório do projeto executar:
```
    set FLASK_APP=app.py
    flask run 
```